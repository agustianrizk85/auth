# env & secrets
.env

# Ed25519 signing keys — NEVER commit the private key.
keys/
*.pem

# build output (root binaries only — NOT the cmd/server source dir)
auth-server
/server
/devserver
*.exe

# AI assistant secret key (set via UI) + logs
data/openrouter.key
*.log
